#!/usr/bin/env bash
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd)"
source ${DIR}/cfg
${DIR}/clean
docker-compose build app
docker-compose run -d --name ${NAME} app
docker exec -it ${NAME} bash
